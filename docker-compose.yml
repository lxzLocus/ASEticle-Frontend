services:
  node:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: front
    ports:
      - "3000:3000"
    #restart: always
    tty: true
    volumes:
      - /app/node_modules
      - type: bind
        source: ./
        target: /app